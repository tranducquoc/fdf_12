$('.reviews-of-post').prepend(
  '<%= j render "ads/posts/review", review: @review %>'
);

$('#ads-review').val('');
<% if @status == false %>
  swal(I18n.t("ads.post.review.fail"), '', 'warning');
<% end %>
