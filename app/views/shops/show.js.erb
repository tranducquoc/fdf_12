$('.list-products-shop-domain').append('<%= j render @products %>');
<% if @products.last_page? %>
  $('.load-more-products').remove();
<% else %>
  $('.load-more-products').html('<%= j link_to_next_page(@products, t("view_more_products")) %>');
<% end %>
